<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:ChecklistApp.Controls"
             x:Name="this"
             x:Class="ChecklistApp.Controls.ChecklistMetaEditor">
    <VerticalStackLayout BindingContext="{x:Reference this}">
        <controls:RoundedEntry Text="{Binding Name}" />
        <Grid Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <controls:RoundedCheckbox Grid.Column="0" HeightRequest="50" IsChecked="{Binding UseDeadline}" />
        </Grid>
    </VerticalStackLayout>
</ContentView>
